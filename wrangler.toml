name = "schadencheck-pages"
type = "javascript"
account_id = ""
workers_dev = true
main = "src/index.ts"

[env.production]
name = "schadencheck-pages-prod"

# Environment variables for production
[env.production.vars]
ENVIRONMENT = "production"

# Secrets are configured separately via:
# wrangler secret put RESEND_API_KEY --env production

# KV Namespace for rate limiting (future enhancement)
# [[kv_namespaces]]
# binding = "RATELIMIT_KV"
# id = "YOUR_KV_ID"
# preview_id = "YOUR_PREVIEW_KV_ID"
